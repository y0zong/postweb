module.exports = {
  plugins: {
    tailwindcss: {
      <% if (management === 'nx') { %>
      config: require('path').join(__dirname, 'tailwind.config.js')
      <% } %>
    },
    autoprefixer: {},
  },
}
